type TravelCardProps = {
  title: string;
  season: string;
  items: string[];
}

const TravelCard = ({title, season, items} : TravelCardProps) => {
  return(

  );
}

function App() {
  return (
    <main>

    </main>
  );
}
export default App;