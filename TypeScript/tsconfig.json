{
  "compilerOptions": {
    /* 기본 설정 */
    "target": "es2016",                                  /* 변환될 JS 버전 (es5, es6, es2016 등) */
    "module": "commonjs",                                /* 모듈 시스템 (Node.js 환경은 commonjs) */
    "rootDir": "./src",                                  /* TS 소스 파일 위치 */
    "outDir": "./dist",                                  /* 컴파일된 JS 파일이 저장될 위치 */
    
    /* 엄격한 타입 검사 (매우 중요) */
    "strict": true,                                      /* 모든 엄격한 타입 검사 옵션 활성화 */
    "noImplicitAny": true,                               /* any 타입 명시 강제 */
    
    /* 모듈 해석 및 호환성 */
    "esModuleInterop": true,                             /* CommonJS와 ES모듈 간의 호환성 보장 */
    "skipLibCheck": true,                                /* 라이브러리 정의 파일(.d.ts) 검사 생략 (속도 향상) */
    "forceConsistentCasingInFileNames": true             /* 파일명 대소문자 구분 */
  },
  "include": ["src/**/*"],                               /* 컴파일할 파일 경로 */
  "exclude": ["node_modules"]                            /* 컴파일 제외 경로 */
}